import { describe, it } from 'vitest';

describe('FindUserUseCase Unit Tests', () => {
  it.todo('deve retornar um usuário pelo id quando ele existir');
  it.todo('deve retornar null quando o usuário não existir');
  it.todo('deve lançar erro quando o id não for informado');
});
